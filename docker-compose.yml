services:
  app:
    build: .
    image: jobpilot-ai:latest
    container_name: jobpilot-ai
    ports:
      - "8501:8501"
    volumes:
      - job-data:/app/db
    environment:
      DB_URL: sqlite:////app/db/jobs.db
      JOBPILOT_LOG_LEVEL: ${JOBPILOT_LOG_LEVEL:-INFO}
      STREAMLIT_SERVER_PORT: 8501
      STREAMLIT_SERVER_ADDRESS: 0.0.0.0
      STREAMLIT_SERVER_HEADLESS: "true"
      STREAMLIT_BROWSER_GATHER_USAGE_STATS: "false"
      PYTHONUNBUFFERED: 1
      PYTHONDONTWRITEBYTECODE: 1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  job-data:
